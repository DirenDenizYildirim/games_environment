# No code yet — this is a docs-and-protocol repo. These entries exist so the
# first coding session in September does not have to think about it.

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Run artifacts. Manifests are the record; raw outputs are not committed.
runs/
artifacts/
outputs/
*.parquet
*.npz
*.ckpt

# JVM, in case the TAG question (ledger G-7) resolves toward the JVM
build/
.gradle/
target/
*.class
*.jar

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code local overrides
.claude/settings.local.json

# Session floor stamped by the SessionStart hook, read by the Stop hook
.claude/.session-start
